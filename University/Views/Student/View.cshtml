@model IEnumerable<Student>
@{
    Layout = "_UniLayout";
}
<div class="dash-content">
    <div class="overview">
        <div class="title">
            <i class="uil uil-tachometer-fast-alt"></i>
            <span class="text">All Students</span>
        </div>
    </div>
    <div>
        <table class="table table-striped">
            <thead>
                <tr>
                    <td class="table-light">Id</td>
                    <td class="table-light">First Name</td>
                    <td class="table-light">Last Name</t>
                    <td class="table-light">Email</t>
                    <td class="table-light">Major</t>
                    <td class="table-light">Operation</t>

                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.FName</td>
                        <td>@item.LName</td>
                        <td>@item.Email</td>
                        <td>@item.Major</td>
                        <td>


                            <a class="btn btn-success" asp-action="Update" asp-route-id="@item.Id" role="button">
                                <span class="material-symbols-outlined">
                                    Edit
                                </span>
                            </a>
                            <a class="btn btn-danger " asp-action="Delete" asp-route-id="@item.Id" role="button">
                                <span class="material-symbols-outlined">
                                    Delete
                                </span>
                            </a>
                            <a class="btn btn-success " asp-action="AssignCourse" asp-route-id="@item.Id" role="button">
                                <span class="material-symbols-outlined">
                                    AssignCourse
                                </span>
                            </a>
                        </td>

                    </tr>
                }


            </tbody>
        </table>

    </div>




</div>